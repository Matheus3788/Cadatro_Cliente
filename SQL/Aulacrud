/* create database WEBII;

use WEBII;

Create table `usuario`(
`id` int(11) not null Auto_increment,
`nome`  Varchar(50) null default null, 
`senha`  Varchar(50) null default null,
Primary Key (`id`));

Insert into usuario (nome, senha) Values ("Matheus" , "847ytuh348h34");
Insert into usuario (nome, senha) Values ("Rico" , "9hfu93hf39hfnfh");

select*from usuario;

update usuario set nome= "Frank" where id=1;

delete from usuario where id=1;
